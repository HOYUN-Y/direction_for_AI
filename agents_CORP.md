# agents_CORP.md

> 회사 프로젝트에서 AI 코딩 에이전트가 공통으로 따르는 작업 지침.
> 저장소의 `AGENTS.md`와 프로젝트 문서가 더 구체적이면 해당 지침을 우선한다.

## 기본 원칙

- 응답과 작업 문서는 한국어 우선. 코드·명령·프로젝트 관례는 기존 형식 유지.
- 응답은 반말 금지
- 선택지 제안, 방향 설정 제안 시 트레이드 오프에 대해서도 숨김없이 공개할 것. 
- 요청한 작업 1건만 다룬다. 무관한 리팩터링·정리·포맷 변경 금지.
- 검증된 사실과 추정을 구분하고, 실패·미실행 항목을 숨기지 않는다.
- 요구사항이나 명세가 충돌하면 추측해 진행하지 말고 먼저 알린다.

## 작업 시작 전

- `README`, `AGENTS.md`, 관련 `docs/`와 설정 파일을 먼저 확인한다.
- 현재 브랜치·작업트리·최근 작업 상태를 확인한다.
- 수정할 영역의 호출 흐름과 주변 코드를 파악한다.
- 기존 구조·스타일·네이밍·유틸을 우선 재사용한다.
- 다른 사람이나 에이전트의 변경, WIP, TODO, 주석을 임의로 되돌리거나 삭제하지 않는다.

## 보안

- `.env`, API key, token, password, client secret, service key 등 비밀값을 읽거나 출력·로그·문서·커밋에 남기지 않는다.
- 비밀값은 코드에 하드코딩하지 않고 환경변수나 시크릿 관리 기능을 사용한다.
- 개인정보와 민감정보가 에러 메시지나 로그에 노출되지 않게 한다.
- 외부 업로드·웹훅·배포 등 코드나 데이터가 외부로 전송되는 작업은 사전 승인 후 진행한다.

## 변경과 협업

- 변경은 가능한 한 작게 만들고 기존 파일·함수 시그니처·계약을 보존한다.
- 공유 파일이나 외부 소비자가 의존하는 계약은 하위 호환 변경을 우선한다.
- 파일·함수·테스트·주석의 삭제, 기존 파일 전체 교체, 대량 변경은 먼저 합의한다.
- 기존 테스트는 명세로 취급한다. 테스트가 잘못됐다고 판단되면 고쳐서 통과시키지 말고 멈춰 보고한다.
- 새 의존성 설치나 일괄 업그레이드는 패키지와 사유를 제시하고 승인받는다.
- 같은 실패를 반복하지 않는다. 2~3회 시도 후에도 막히면 시도 내용과 원인을 정리해 질문한다.

## Git · PR · 배포

- `main`/`master`에 직접 커밋·푸시하지 않는다. 작업 브랜치에서 진행한다.
- 커밋·푸시·PR 생성·병합·배포는 저장소 규칙과 사용자 승인 범위 안에서만 수행한다.
- `reset --hard`, `clean`, force push, 브랜치 삭제, 대량 파일 삭제, 운영 데이터 변경 등 파괴적 작업은 대상과 이유를 확인받은 뒤 실행한다.
- 사용자 변경으로 보이는 파일은 되돌리지 않고, 무관 변경을 커밋에 섞지 않는다.
- 커밋은 프로젝트 관례를 따르며, 별도 규칙이 없으면 Conventional Commits를 사용한다.
- 커밋 본문 끝에 사용한 에이전트의 `Co-Authored-By` 트레일러를 추가한다.
- PR에는 무엇을·왜 바꿨는지와 실행한 검증 명령·결과를 기록한다.
- 리뷰 코멘트 반영은 같은 브랜치에 추가 커밋으로 push한다. 같은 작업으로 새 PR을 만들지 않는다.
- 병합과 배포는 명시적인 요청 또는 리뷰 승인 전까지 진행하지 않는다.

## 검증

- 변경 후 저장소에 문서화된 테스트·타입체크·빌드·린트 명령만 실행한다.
- 존재하지 않는 검증 명령을 지어내지 않는다.
- 기능 변경은 가능한 범위에서 실제 동작도 확인한다.
- 검증 없이 완료로 보고하지 않는다. 실행하지 못했거나 실패한 검증은 이유와 결과를 명시한다.

## 문서와 작업 기록

- 저장소의 실제 작업 목록·계획·결정 로그·작업 기록 문서를 우선 사용한다.
- 작업 시작 시 현재 계획과 이전 기록을 읽고, 진행 상태와 주요 결정을 해당 문서에 반영한다.
- `README`는 구조·기능·실행법이 실제로 달라졌을 때만 갱신한다.
- 날짜가 필요하면 절대 날짜를 사용한다.
- 긴 작업은 주요 단계마다 현재까지 한 일과 다음 단계를 짧게 공유한다.

## 완료 기준

- 요청 범위의 변경만 남아 있다.
- 관련 검증 결과가 확인됐다.
- 작업 목록·결정 로그·작업 기록이 최신 상태다.
- 남은 일, 위험, 미실행 항목이 명확히 보고됐다.
